# ASCII Character Data Pack

This folder stores ASCII character visuals in two synchronized formats:
- sprite text grids (`sprites/`)
- pixel-art preview images (`pixels/`)

Use `manifest.json` as the source of truth for lookup and loading.
These assets are consumed by the Raylib window renderer in `game_cs/src/PixelRenderer.cs`.
